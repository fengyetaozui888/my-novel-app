# 设计指南 - 人设管理小程序

## 品牌定位

创作者的角色管理手账，温暖、私密、充满文学气息。帮助小说创作者管理笔下角色的设定信息，并通过 AI 模拟角色对话。

## 配色方案

| 用途 | Tailwind 类名 | 色值 | 说明 |
|------|--------------|------|------|
| 主色 | `bg-rose-500` / `text-rose-500` | #e8587a | 按钮、对话框标题、主要操作 |
| 辅色背景 | `bg-pink-50` | #fce4ec | 卡片背景、选中态 |
| 页面底色 | `bg-stone-50` | #f8f5f2 | 整体页面背景 |
| 标题文字 | `text-gray-900` | #2d2233 | 标题、重要文字 |
| 正文文字 | `text-gray-700` | #4a4a4a | 正文内容 |
| 次要文字 | `text-gray-400` | #9e8e92 | 辅助说明 |
| 主角标识 | `text-rose-500` / `bg-rose-50` | | 主角分类 |
| 配角标识 | `text-purple-500` / `bg-purple-50` | | 重要配角分类 |
| 龙套标识 | `text-slate-400` / `bg-slate-50` | | 不重要角色分类 |

## 组件使用原则

- 通用 UI 组件（Button、Input、Dialog、Card、Tabs、Badge、Toast 等）优先使用 `@/components/ui/*`
- 角色详情编辑使用 Dialog 组件，内部自定义粉色主题样式
- 小说模块使用 Card 组件作为容器
- 角色分类使用 Tabs 或自定义分段切换
- 分类标签使用 Badge 组件

## 间距系统

- 页面边距：`px-4`
- 卡片间距：`gap-3`
- 卡片内边距：`p-4`
- 组件间距：`gap-2`

## 容器样式

- 卡片：`rounded-2xl` + 浅阴影 + 白色/粉色背景
- 对话框：粉色主题，标题区域使用渐变粉色背景
- 底部操作区：固定定位，避开 TabBar

## 导航结构

- 首页（index）：小说模块列表
- 小说详情（novel）：角色分类管理（三列）
- 对话模拟（chat）：角色人设模拟聊天

## 空状态与加载态

- 空状态：居中图标 + 温暖提示文案
- 加载态：Skeleton 骨架屏
